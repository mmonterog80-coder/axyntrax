# JARVIS HUD (React / Vite)
FROM node:20-alpine

WORKDIR /app

COPY jarvis_hud_v2/package*.json ./
RUN npm install

COPY jarvis_hud_v2/ .

EXPOSE 3002

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3002"]
