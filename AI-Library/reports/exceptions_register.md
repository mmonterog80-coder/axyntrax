# Exceptions Register

No active exceptions currently registered.
When a temporary exception is granted (e.g., bypassing a test to hotfix production), it will be logged here with its expiration timestamp.
