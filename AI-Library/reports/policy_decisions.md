# Policy Decisions

All current policy decisions map to the default AXYNTRAX Governance Core.
No external policy engine (like OPA) is currently attached, so all logic relies on Agent internal enforcement mapping to `approval_matrix.json`.
